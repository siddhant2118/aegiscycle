// Local API configuration.
// Fill in `API_BASE` with the root URL of your backend (e.g. "https://api.example.com/v1")
// and `API_KEY` with the credential the backend expects (leave blank if not required).
// Do NOT commit secrets to source control.
export const API_BASE = "";
export const API_KEY = "";

 